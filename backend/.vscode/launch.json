{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Express App",
            "processId": "${command:PickProcess}",
            "request": "attach",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "type": "node"
        }
    ]
}